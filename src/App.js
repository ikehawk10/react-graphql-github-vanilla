import React, { Component } from 'react';

const Title = 'React GraphQL Github Client';

class App extends Component {
  render() {
    return (
      <div>
        <h1>{Title}</h1>
      </div>
    );
  }
}

export default App;
